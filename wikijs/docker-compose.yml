services:
  wikijs:
    image: ghcr.io/requarks/wiki:2
    container_name: wikijs
    ports:
      - 3005:3000
    environment:
      DB_TYPE: sqlite
      DB_FILEPATH: /wiki/data/database.db
    volumes:
      - ./data:/wiki/data
    restart: unless-stopped
